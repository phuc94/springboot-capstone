post:
  summary: Create a new Key
  description: create new Key
  operationId: createAdminKey
  tags:
    - adminGameKey
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../schemas/GameKeyDTO.yaml"
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            $ref: "../schemas/GameKeyDTO.yaml"

get:
  summary: Get all gameKeys
  description: getAll
  operationId: getAllAdminKeys
  tags:
    - adminGameKey
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../schemas/GameKeyDTO.yaml"
