post:
  summary: Create a new game description
  description: createGameDescription
  operationId: createGameDescription
  tags:
    - adminGameDescription
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../schemas/GameDescriptionDTO.yaml"
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            type: object
            properties:
              code:
                type: integer
                example: "200"
              message:
                type: string
                example: "created"
              data:
                $ref: "../schemas/GameDescriptionDTO.yaml"
    "400":
      $ref: "../responses/400.yaml"
    "404":
      $ref: "../responses/404.yaml"

get:
  summary: Get all game descriptions
  description: getAllGameDescriptions
  operationId: getAllGameDescriptions
  tags:
    - adminGameDescription
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            type: object
            properties:
              code:
                type: integer
                example: "200"
              message:
                type: string
                example: "ok"
              data:
                type: array
                items:
                  $ref: "../schemas/GameDescriptionDTO.yaml"
    "400":
      $ref: "../responses/400.yaml"
    "404":
      $ref: "../responses/404.yaml"
