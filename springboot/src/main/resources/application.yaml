spring:
  application:
    name: capstone

  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    org:
      hibernate:
        stat: debug

springdoc:
  api-docs:
    path: /v3/api-docs.yaml
  swagger-ui:
    url: /api-design/api.yaml